'use strict';

describe('myApp.viewTriggerContent module', function() {

  beforeEach(module('myApp.viewTriggerContent'));

  describe('viewTriggerContent controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewTriggerContentCtrl = $controller('ViewTriggerContentCtrl');
      expect(viewTriggerContentCtrl).toBeDefined();
    }));

  });
});