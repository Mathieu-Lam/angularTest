'use strict';

describe('myApp.viewTriggerList module', function() {

  beforeEach(module('myApp.viewTriggerList'));

  describe('viewTriggerList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewTriggerListCtrl = $controller('ViewTriggerListCtrl');
      expect(viewTriggerListCtrl).toBeDefined();
    }));

  });
});